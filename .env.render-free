# CipherCare Backend - Render Configuration
# Uses 768-dimensional embeddings with CyborgDB (local) or Pinecone (cloud)

# ========== Embedding Model ==========
# Uses full-quality 768-dimensional embeddings
# Model: all-mpnet-base-v2 (standard, highest quality)
EMBEDDING_MODEL=sentence-transformers/all-mpnet-base-v2

# ========== Vector Database ==========
# CyborgDB: Local vector database (preferred, encrypts with Vault)
# Pinecone: Cloud vector database (optional, for persistence on Render)
VECTOR_DB_TYPE=cyborgdb

# Pinecone Configuration (only needed if VECTOR_DB_TYPE=pinecone)
# PINECONE_API_KEY=your-api-key-here
# PINECONE_ENV=gcp-starter

# ========== CyborgDB Configuration ==========
# Only needed if VECTOR_DB_TYPE=cyborgdb (default)
CYBORGDB_API_KEY=your-key-here
CYBORGDB_BASE_URL=http://localhost:8002

# ========== PHI Scrubber ==========
# Uses lightweight regex-based PHI detection (no Spacy required)
ENABLE_PHI_SCRUBBING=true

# ========== JWT Configuration ==========
# Generate a random secret key (use: python -c "import secrets; print(secrets.token_urlsafe(32))")
JWT_SECRET_KEY=your-secret-key-here-change-in-production

# ========== Logging ==========
LOG_LEVEL=INFO

# ========== Environment ==========
ENVIRONMENT=production

# ========== Database (Optional PostgreSQL) ==========
# For user data storage (separate from vector embeddings)
# DATABASE_URL=postgresql://user:password@localhost:5432/cipercare

# ========== CORS ==========
CORS_ORIGINS=["http://localhost:3000","https://your-frontend-domain.com"]
