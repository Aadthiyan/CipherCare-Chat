# Core FastAPI dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-dotenv>=1.0.0
python-multipart>=0.0.6

# Database
psycopg2-binary>=2.9.9
sqlalchemy>=2.0.23

# Data validation
pydantic>=2.5.0
email-validator>=2.0.0

# Authentication & Security
bcrypt>=4.0.1
PyJWT>=2.8.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
pyotp>=2.9.0

# Rate limiting
slowapi>=0.1.9

# CyborgDB Embedded (PostgreSQL-backed)
# cyborgdb-lite includes cyborgdb-core as dependency
cyborgdb-lite>=0.12.0

# Embeddings via HF API (no local models!)
numpy>=1.24.0
requests>=2.31.0
huggingface_hub>=0.16.0
sentence-transformers>=2.2.0  # For admin upload endpoint

# Utilities
cryptography>=41.0.0

# Email service (optional)
sib-api-v3-sdk>=7.6.0

# LLM (optional)
groq>=0.4.0

# ✅ MEMORY OPTIMIZED FOR RENDER FREE TIER (512MB)
# Removed: torch, transformers, sentence-transformers (670MB total)
# Using: Hugging Face Inference API instead (0MB local memory!)
